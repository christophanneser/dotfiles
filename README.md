# Install Neovim under Debian
$ sudo apt-get install neovim
## vim-plug
is installed by adding vim/autoload/plug.vim
### language-server 
- cpp: clangd (apt-get install clang-tools)
- python: pip install python-language-server
